spring:
  application:
    name: activity-service
  data:
    mongodb:
      uri: mongodb://mongo:27017/activitydb
  kafka:
    bootstrap-servers: localhost:9092

mongock:
  migration-scan-package:
    - com.innowise.activityservice.migrations